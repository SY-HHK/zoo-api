# zoo-api
Zoo api with node and typescript

## Installation

Copy and configure .env

```shell
cp .env.dev .env
```

Install packages

```shell
npm install
```

Init database

```shell
npm run create-schema
```
